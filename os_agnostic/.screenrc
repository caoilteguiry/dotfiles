escape ^Xx     # I tend to use C-a to jump to the start of the line a lot, so lets use C-x instead
shell -$SHELL    
caption always "%{= kw}%-w%{= BW}%n %t%{-}%+w %-= @%H - %LD %d %LM - %c"
defscrollback 5000
startup_message off
